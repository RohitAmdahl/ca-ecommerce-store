import { styled } from "styled-components";

const Wrapper = styled.section`
  width: 100%;
  max-width: 900px;
  margin: 0 auto;
  padding: 1rem;
  .hero-imge {
    width: 100%;
    max-width: 900px;
    margin: 0 auto;
  }
`;

export default Wrapper;
