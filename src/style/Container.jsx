import styled from "styled-components";
const Card = styled.div`
  width: 100%;
  max-width: 900px;
  margin: 0 auto;
  display: flex;
  .title {
    padding: 0 1rem;
  }
  .card {
    background: ;
    display: flex;
    flex-direction: column;
  }
  .card-img {
    width: 100%;
    max-width: 250px;
    padding: 0.8rem;
  }

  .card-body {
  }
  .price {
    padding: 0 1rem;
    display: flex;
    gap: 1rem;
  }
`;
export default Card;
